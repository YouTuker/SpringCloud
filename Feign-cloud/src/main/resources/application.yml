server:
  port: 10010

spring:
  application:
    name: Feign-Cloud
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848


management:
  endpoint:
    web:
      exposure:
        incloud: '*'