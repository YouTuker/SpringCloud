server:
  port: 9002


spring:
  application:
    name: cloudAlibaba-provide-payment
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

management:
  endpoint:
    web:
      exposure:
        incloud: '*'